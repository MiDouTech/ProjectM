/**
 * 组织/RBAC：用户/部门/角色/权限码/数据范围/身份映射
 *
 * <p>模块内分层（CLAUDE.md §4）：controller / service / domain / mapper / entity / dto / event。
 * 跨域只经 Service 接口或领域事件交互，禁止跨域直接查表。骨架阶段，业务实现待填充。</p>
 */
package com.mido.pm.org;
